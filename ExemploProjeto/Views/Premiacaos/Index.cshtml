@model IEnumerable<ProjetoCinema.Models.Premiacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tituloFilme)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Filme)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tituloFilme)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Filme.idFilme)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idPremiacao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idPremiacao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idPremiacao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
